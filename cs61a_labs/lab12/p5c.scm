;
; Double-quote would be the same as Scheme's single quote. However, now you cannot use two double quotes to represent a 'string' in Logo.
; Square brackets would be the same as constructing a sequence of words, no quote(s) are needed in Logo. 
; Colons in Logo are used to denote variables.
;