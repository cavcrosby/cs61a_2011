; When Alyssa attempts to use the newly defined new-if procedure, it will go into infinite recursion. New-if is not treated as a special form, hence, will evaulate its arguments before using them in the procedure application.